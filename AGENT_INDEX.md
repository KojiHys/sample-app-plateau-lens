# AGENT_INDEX — PLATEAUハッカソン サンプルアプリ

この案件でセッションを開始したエージェント向けの索引。タスク別に読む順序を示す。

## 案件の状態

| 項目 | 内容 |
|---|---|
| フェーズ | 企画完了 → 実装待ち |
| **リポジトリ名** | **`sample-app-plateau-lens`** |
| 表示名 | PLATEAU Lens |
| 開催日 | **2026-09-26（土）** |
| 実装可能日 | 2026-09-25（金）のみ |
| 企画確定日 | 2026-09-24 |

## タスク別の参照先

| タスク | 読む順序 |
|---|---|
| **実装する** | `output/proposal/実装引き継ぎ書_20260924.md` を**単独の作業指示として読む**。→ 第1章の事前検証3件を最初に実施 → Tier 1 → Tier 2 → Tier 3。判断の経緯を再検討する必要はない |
| 設計判断の背景を知る | `context.md` → `output/proposal/サンプルアプリ企画_20260924.md` |
| PLATEAUの技術前提を確認する | `knowledge/2026-09-24_mlit-plateau-3d-city-model-overview.md`（ワークスペース直下） |
| 企画を変更する | `context.md` の決定事項 → 企画書の該当章 → 引き継ぎ書を同期 |

## 成果物

| ファイル | 内容 | 用途 |
|---|---|---|
| `context.md` | 案件前提、決定事項、検証済み事項、ブロッカー | 起点 |
| `output/proposal/サンプルアプリ企画_20260924.md` | 企画の全体像。3パターン比較、ADR、W-A 6柱、リスク表、デモストーリー | 判断の根拠 |
| `output/proposal/実装引き継ぎ書_20260924.md` | 具体仕様のみ。属性名・レンジ、API契約、DynamoDB設計、実装順序、完成確認チェックリスト | **実装時はこれだけ読めばよい** |

## 確定済みの重要判断（実装時に覆さない）

1. **構成は Pattern A**: S3+CloudFront / Cognito / API Gateway HTTP API / Lambda / DynamoDB のみ
2. **3D描画とフィルターは100%クライアントサイド**。配信サービスのCORSが `*` であることを実測済みのため、Lambdaによるプロキシは作らない
3. **認証の対象は本アプリが追加する書き込みパスと一覧API**。PLATEAUデータ（公開オープンデータ）を認証で守ろうとしない
4. **読み取りは公開、書き込みと一覧は認証**
5. **一覧は自分のビューのみ**。所有者は `event.requestContext.authorizer.jwt.claims.sub` から取得し、クライアント入力を信用しない（IDOR対策）
6. **Tier 1（バックエンドなし）を完成・デプロイしてから Tier 2 を積む**
7. **出典表記は削らない**（PLATEAUのライセンス上の必須事項）
8. Lambda@Edge による静的ファイルの認証保護は**不採用**
9. **全AWSリソースの接頭辞は `sample-app-plateau-lens`** に統一する（短縮しない）。公式ロゴ・公式カラーは使わず、サブタイトルに「非公式」を明記する

## 除外対象（読まなくてよい）

- ワークスペース内の他案件フォルダ（`projects/006RU00000HSsjeYAD_UR都市機構_R10更改/` 等）は本案件と無関係
- `knowledge/` のうち PLATEAU 以外のファイル

## 未確定（実装をブロックしない）

- 主催者側の要望（テーマ、参加者スキルレベル、アカウント発行方法）
- ハッカソン後のリソース削除日
